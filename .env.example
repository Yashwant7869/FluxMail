# SheetMailEngine Environment Variables

# Port for the server (default: 5000)
PORT=5000

# SendGrid Configuration (required for email sending)
# Get your API key from: https://app.sendgrid.com/settings/api_keys
# Required permissions: Full Access or Mail Send
# Must verify sender email at: https://app.sendgrid.com/settings/sender_auth/senders
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Google Sheets API Configuration (required for Google Sheets integration)
# Get your API key from: https://console.developers.google.com/
# Enable Google Sheets API at: https://console.cloud.google.com/apis/library
GOOGLE_SHEETS_API_KEY=your_google_sheets_api_key_here
# Alternative name for Google API key
GOOGLE_API_KEY=your_google_api_key_here

# Database Configuration (optional - uses in-memory storage by default)
# For development, you can use a local PostgreSQL instance
# For production, use a hosted database service like Neon, Supabase, or PlanetScale
# DATABASE_URL=postgresql://username:password@localhost:5432/sheetmailengine

# Node Environment
NODE_ENV=development